{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build Rust WASM and Start Dev Server",
			"type": "shell",
			"command": "cd rust-wasm && wasm-pack build --target web --out-dir ../pkg && cd .. && npm run dev",
			"group": "build",
			"isBackground": true,
			"problemMatcher": [
				"$tsc"
			],
			"args": []
		}
	]
}